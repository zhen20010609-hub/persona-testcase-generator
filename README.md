1. 此代码用于生成persona-api基础格式校验部分的测试用例，支持生成明文&md5格式用例
2. country = PH时，会显示id_type输入栏
3. 如需要拓展key，请选择是后输入key名，同时在下面value输入对应值，用英文逗号分隔
4. 用例场景说明：此工具适用于生成海外基础数据产品的基础格式校验部分的公共校验测试用例，多个扩展key手动输入入参，或单个扩展key（除id, cell, name外）通过txt读取入参（适用于超大base64串入参的场景）
5. 当前工具仅支持生成用例本体，预期需要自行根据产品文档进行填写
